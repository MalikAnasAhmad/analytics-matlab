% % read the data from csv file
% C_20190913_1500_reading_ware_house_data
% 
% recordings = 1000;
% action = action(1:recordings);
% company_space = company_space(1:recordings);
% env = env(1:recordings);
% id = id(1:recordings);
% ip = ip(1:recordings);
% platform = platform(1:recordings);
% response_time = response_time(1:recordings);
% sequence_id = sequence_id(1:recordings);
% session_id = session_id(1:recordings);
% time = time(1:recordings);
% user_email = user_email(1:recordings);

% action_q = make_quantize_index_for_cell_vector(action);
% % action_q = str2num(make_quantize_index_for_cell_vector(action));
% activity_session_id_q = str2num(make_quantize_index_for_cell_vector(activity_session_id));
% company_space_q = str2num(make_quantize_index_for_cell_vector(company_space));
% env_q = str2num(make_quantize_index_for_cell_vector(env));
% id_q = str2num(make_quantize_index_for_cell_vector(id));
% ip_q = str2num(make_quantize_index_for_cell_vector(ip));
% response_time_q = str2num(make_quantize_index_for_cell_vector(response_time));
% sequence_id_q = str2num(make_quantize_index_for_cell_vector(sequence_id));
% session_id_q = str2num(make_quantize_index_for_cell_vector(session_id));
% time_q = str2num(make_quantize_index_for_cell_vector(time));
% user_email_q = str2num(make_quantize_index_for_cell_vector(user_email));

hash_map_action = make_hash_map_container_for_vector_cell(action);